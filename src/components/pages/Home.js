import Nav from '../Nav';
import SearchBar from '../SearchBar';
import Trending from '../Trending';

const Home = () => {
    return (
        <div>
            <Nav />
            <SearchBar />
            <Trending />
        </div>
    );
};

export default Home;
